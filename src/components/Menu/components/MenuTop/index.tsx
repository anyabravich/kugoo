import React from "react";
import styles from "./styles.module.scss";
import Container from "@/components/Container";

const MenuTop = () => {
  return (
    <section className={styles["menu-top"]}>
      <Container>MenuTop</Container>
    </section>
  );
};

export default MenuTop;
